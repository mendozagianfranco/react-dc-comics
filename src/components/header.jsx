import Menu from './Menu';

export default function Header() {
    return (
        <header>
            <img src="/dc-logo.png" alt="Logo DC comics" />
            <Menu />
        </header>
    );
}